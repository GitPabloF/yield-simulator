<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin - Simulations | Emerald Stay</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/styles.css" />
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-light bg-white border-bottom py-3">
        <div class="container-fluid px-4">
            <a class="navbar-brand" href="https://emeraldstay.com">
                <img src="/icons/EmeraldStay_Logo.svg" alt="Emerald Stay" height="32" />
            </a>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="py-4">
        <div class="container-fluid px-4">

            <!-- Header -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3 mb-0">Simulations</h1>
                <a href="/" class="btn btn-outline-secondary btn-sm">← Back to Simulator</a>
            </div>

            <!-- Table -->
            <div class="card">
                <div class="card-header bg-white py-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <span class="fw-semibold">All Simulations</span>
                        <button class="btn btn-sm btn-outline-primary" onclick="loadSimulations()">
                            Refresh
                        </button>
                    </div>
                </div>
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Date</th>
                                <th>Email</th>
                                <th class="text-end">Purchase Price</th>
                                <th class="text-end">Monthly Rent</th>
                                <th class="text-end">Return (3Y)</th>
                                <th class="text-center">Data-Driven</th>
                            </tr>
                        </thead>
                        <tbody id="simulationsTable">
                            <tr>
                                <td colspan="6" class="text-center py-4 text-muted">Loading...</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </main>

    <!-- Footer -->
    <%- include('./partials/footer') %>


        <script src="/js/admin.js"></script>
</body>

</html>